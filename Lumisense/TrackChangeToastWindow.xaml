<Window x:Class="Lumisense.TrackChangeToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Lumisense"
        Title=""
        Width="300" Height="72"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="True"
        WindowStartupLocation="Manual">

    <!-- Уведомление о смене трека (как у Spotify): ShowActivated="False", чтобы не забирать фокус; окно переиспользуется (ShowToast).
         Ресурсы темы заданы здесь, как в MiniPlayerWindow: фон рисуется вручную (не Mica/Acrylic), контраст текста — тоже. -->

    <Window.Resources>
        <SolidColorBrush x:Key="TextFillColorPrimaryBrush" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="TextFillColorSecondaryBrush" Color="#C5FFFFFF" />
        <SolidColorBrush x:Key="ControlFillColorSecondaryBrush" Color="#1AFFFFFF" />
    </Window.Resources>

    <Border x:Name="RootBorder" CornerRadius="12" Padding="12" Opacity="0"
            MouseLeftButtonDown="RootBorder_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.35" BlurRadius="18" ShadowDepth="4" Direction="270" />
        </Border.Effect>
        <Border.Background>
            <SolidColorBrush x:Name="ToastBackgroundBrush" />
        </Border.Background>

        <!-- DockPanel, а не StackPanel: сторона обложки и выравнивание текста настраиваются (AppSettings.TrackChangeToast*) в ApplyLayout;
             LastChildFill отдаёт текстовой колонке всё оставшееся место. -->
        <DockPanel LastChildFill="True">
            <Border x:Name="ArtBorder" DockPanel.Dock="Left" Width="48" Height="48" CornerRadius="8"
                    Background="{DynamicResource ControlFillColorSecondaryBrush}"
                    SnapsToDevicePixels="True" UseLayoutRounding="True">
                <!-- Отдельный Image вместо ImageBrush в Border.Background: на маленькой карточке WPF может терять качество при масштабировании,
                     а Image (как в мини-плеере) всегда использует HighQuality. -->
                <Grid ClipToBounds="True">
                    <Image x:Name="ArtImage" Stretch="UniformToFill" Visibility="Collapsed"
                           RenderOptions.BitmapScalingMode="HighQuality"
                           RenderOptions.EdgeMode="Unspecified"
                           SnapsToDevicePixels="True" UseLayoutRounding="True" />
                    <local:SvgPathIcon x:Name="ArtIcon" Icon="IconMusicNote" Size="20" Opacity="0.5"
                                       HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Grid>
            </Border>

            <StackPanel x:Name="ToastTextPanel" VerticalAlignment="Center" Margin="12,0,0,0" MaxWidth="200">
                <TextBlock x:Name="ToastTitleText" FontSize="13" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                           TextTrimming="CharacterEllipsis" />
                <TextBlock x:Name="ToastArtistText" FontSize="11" Margin="0,3,0,0"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           TextTrimming="CharacterEllipsis" />
            </StackPanel>
        </DockPanel>
    </Border>
</Window>
